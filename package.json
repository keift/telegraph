{
  "scripts": {
    "start": "bun run ./src/main.ts",
    "start:dev": "bun run ./src/main.ts",
    "tests": "bun run lint && bun test",
    "prettier": "prettier --write ./",
    "lint": "eslint ./"
  },
  "dependencies": {
    "@types/telegraph.md": "^0.0.0",
    "axios": "^1.13.2",
    "prettier": "^3.8.0",
    "rulint": "^1.0.5",
    "telegraph.md": "^0.0.1"
  }
}
